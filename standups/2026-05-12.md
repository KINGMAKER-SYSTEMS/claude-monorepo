# Morning Standup — 2026-05-12 (Tue)

_Generated by morning-stand-up scheduled task | 2026-05-12 (Tue morning)_

## 🚨 BRAIN MCP DOWN — full structural risk

All brain tools (`brain_doctor`, `brain_standup`, `brain_alerts`, `brain_open_loops`, `brain_git_dirty`, `brain_transcripts_recent`, `brain_projects`) returned `Command failed with no output` today. Same failure mode as 2026-04-28. Standup rebuilt entirely from live `git`, `gh`, Gmail. **5 brain-degraded standups now in 17 days** — the silent-failure indexer-staleness pattern from 5/11 is no longer hypothetical. Brain is unreliable.

---

## 🔁 DECIDE NOW (carryovers ≥5 standups)

| Item | Standups | Why it's still here |
|---|---|---|
| **iMessage allowlist** | **8** | Auto-skipped again. Spec needs formal removal from `10-gather.md`. Trivial edit blocked 8 days. |
| **Deployment status probe** (Railway / Vercel / Supabase / Cloudflare) | **11** | 11th carry. Today brain-down means **zero deploy visibility** beyond what `gh` shows. |
| **PR-decision queue** | **6** | PR #5 (26d idle), PR #24 (5d MERGEABLE), plus #41/#42 opened today and already draft-blocked on each other. Pattern is structural. |
| **Anthropic billing thread** | **4** | No human reply now 6 days. Decide: escalate or cap. |

---

## 🚨 LIVE ISSUES (past 48h, must act today)

| # | Issue | Source | Status |
|---|---|---|---|
| 1 | **Brain MCP fully down** — all 7 brain tools return empty. Repeat of 2026-04-28 failure mode. Without the live `git`+`gh`+Gmail fallback this standup would have been empty. | Direct tool calls | ⚠️ NEEDS JOHN — restart brain daemon / check postgres / triage upstream. Recurring fault. |
| 2 | **content-posting-lab — origin/main shipped 4 more commits since 5/11 (#37, #38, #40 + commit 6a5b102).** That's now **19+ commits in 5 days** on the most-shipped project. Local branch `feature/sounds-message-html-links` is still `[gone]` upstream and uncommitted `lazy-Rosana/` work has not been rebased or stashed for 5+ days. | `git log origin/main --since=2026-05-07` | ⚠️ NEEDS JOHN — `[gone]` upstream means naive push creates a new branch. Still requires intent. 2nd standup carrying. |
| 3 | **2 new PRs on campaign-hub opened today** — PR #41 (`Wire up optimistic ticked-row UI with undo + fix build`, draft, MERGEABLE) and PR #42 (`Scaffold vitest + smoke tests`, draft, CONFLICTING). Both reference the same Claude session. **#42 conflicts with main**, **#41 fixes the build that #42's tests would otherwise see broken** — order of merge matters. | `gh pr list` | ⚠️ NEEDS JOHN — review-and-merge #41 first, then rebase #42. ~10 min. |
| 4 | **PR #5 (campaign-hub Telegram PRD) — 26 days idle, mergeable: UNKNOWN, 5 §12 questions still unanswered.** | `gh pr view 5` | ⚠️ NEEDS JOHN — 4th standup carrying. Answer the 5 questions or close the PR. |
| 5 | **content-posting-lab PR #24** (regenerate CLAUDE.md) — 5 days MERGEABLE, single .md file, no checks failing. | `gh pr view 24` | ⚠️ NEEDS JOHN — 3rd standup carrying. 30-sec self-merge. Just below the 7-day docs-only auto-merge threshold; will be auto-mergeable tomorrow if untouched. |
| 6 | **claude-monorepo Week 1 scaffold STILL uncommitted, 5 days now** — same 5 modified files + new `0003_tracked_changes.sql` migration as 5/11. Sitting on real schema work for 5 days. | `git status -sb` | ⚠️ NEEDS JOHN — 2nd standup carrying. Commit or revert. |
| 7 | **rising-tides-course `customer-support` — Stripe/D1 WIP, still +3 / -10 vs origin/main, 28 days stale.** | Live `git status` | ⚠️ NEEDS JOHN — 3rd standup carrying. Touches paid Stripe webhook + production D1 schema. |
| 8 | **Anthropic billing thread** still no human reply (6+ days). | Gmail | ⚠️ NEEDS JOHN — 4th standup. Escalate or cap. |

**Resolved since 5/11:**
- PR #12 (`fix(scraping): unblock original-sound discovery`) — **CLOSED** today 20:32 UTC. 5/07 architectural decision finally landed as "abandon." Removed from carryovers.

---

## 🤖 ACTIONS TAKEN OVERNIGHT

- ✅ **PR opened**: [`KINGMAKER-SYSTEMS/tidestracker#5`](https://github.com/KINGMAKER-SYSTEMS/tidestracker/pull/5) — `chore: gitignore .claude/ to stop status noise`. Tier B, scope-bounded sub-agent, single-file change to `.gitignore`. Stops the recurring `?? .claude/worktrees/` noise that has appeared in 3+ standups.
- ✅ **PR opened**: [`Risingtides-dev/risingtides-campaign-hub#43`](https://github.com/Risingtides-dev/risingtides-campaign-hub/pull/43) — `chore: gitignore .claude/ to stop status noise`. Same fix for the campaign-hub repo which has 120+ agent worktrees polluting `git status`.
- ✅ **Live `git` cross-check** caught: (a) 4 more `origin/main` commits on content-posting-lab since 5/11 standup, (b) PR #12 was CLOSED today, (c) 2 brand-new PRs (#41, #42) on campaign-hub opened in the past hour, (d) rising-tides-course main unchanged since 5/07.
- ✅ **WO DoD audit** ran by hand (script broken on zsh `status` readonly var, see Pipeline TODOs): WO-01 0/4, WO-02 0/7, WO-03 0/7, WO-04 SUPERSEDED (✅ from 5/07), WO-05 0/6. **4 active WOs all 0% — same as 5/11. Pattern: WOs are write-only.**
- ✅ **PR #12 carryover RESOLVED**: closed today, no longer needs Tier B rebase or comment.
- 📝 **Improvement note appended** to `STANDUP_IMPROVEMENT_NOTES.md` with today's findings.

_2 PRs opened, no Tier C draft posts, no orphan kills (active dev servers couldn't be safely attributed to dead sessions without brain), no new WOs. All remaining LIVE ISSUES require John-level decisions (rebases, commit-vs-revert, architectural calls)._

---

## 🧭 WHERE I LEFT OFF

Brain MCP is down — no transcript data available. Reconstructed from live PR/git signals:

### 1. `risingtides-campaign-hub` — Claude session `017EuAzYEwShfNcKueEkWz7H` (active today)
PRs #41 + #42 opened in the past hour from this session. Work focused on `frontend/src/pages/ScrapeTasks.tsx` (optimistic ticked-row UI) and the vitest scaffold. Local branch is still `feat/link-tracker-to-campaign` with the WIP from before — separate from this session's main-based PRs.

### 2. `content-posting-lab`
No local commits or session signal. 4 more commits shipped to `origin/main` (pipeline intake, sounds clickable links merge, email-routing handoff) — implies someone is actively shipping production fixes elsewhere. Local worktree at `/Users/risingtidesdev/dev/content-posting-lab` is `[gone]` upstream.

### 3. `claude-monorepo`
Worktree `friendly-torvalds-e01863` is this run. No other recent work on this monorepo's `main` — 5/07 standup PR commit is still HEAD on origin/main. The Week 1 brain-indexer scaffold from 5/07 is still uncommitted in the gitbutler workspace.

---

## 🔁 CARRYOVERS

| Item | Standups Carrying | Status |
|---|---|---|
| Deployment status probe | **11** | 🟥 STILL CARRYING — brain-down today made this acutely load-bearing. |
| iMessage allowlist | **8** | 🟥 Auto-skipped. Spec edit still pending. |
| Carryover counter as code | **6** | 🟥 Hand-counted again. |
| DoD auto-audit as bash | **6** | 🟥 Hand-counted again; my zsh script broke on `status` readonly var. |
| Brain indexer staleness alarm | **2** | 🟥 Today brain is fully down, not just stale. Promoted from "behind" to "off." |
| Anthropic billing thread | **4** | ⚠️ Still no human reply. |
| Live `git status` re-verification | 5 | ✅ Done today — caught PR #12 closure + 4 new content-posting-lab commits + 2 new campaign-hub PRs that brain couldn't see. Permanent step. |
| Cross-org PR enumeration | 4 | ✅ Done today — caught PRs #41 / #42. Permanent step. |
| Cron install verification | 4 | ⚠️ Still unverified after 5 days. `crontab -l` not in pipeline. |
| Postiz × TikTok WO-05 | unknown | No fresh Postiz emails in 6 days. Without deploy probe, can't tell if resolved or paused. |
| content-posting-lab origin/main drift | **2** | 🟥 Worse today: +4 more commits since 5/11. |
| claude-monorepo Week 1 uncommitted | **2** | 🟥 Zero change in 5 days. |
| PR #5 (campaign-hub PRD) | **4** | ⚠️ 26 days idle. |
| PR #24 (content-posting-lab docs) | **3** | ⚠️ 5 days MERGEABLE. Eligible for docs-only auto-merge tomorrow at 7d if untouched. |
| PR #12 architectural call | **2** | ✅ RESOLVED — closed today. |
| rising-tides-course Stripe/D1 WIP | **3** | ⚠️ 28 days stale. |

---

## 🎯 TODAY'S FOCUS (ranked, things John must handle)

1. **Restart brain MCP** — `brain_doctor` is dead, no debug signal. ~5 min. Recurring fault.
2. **campaign-hub — merge PR #41 first, then rebase + merge PR #42.** Order matters because #42's vitest scaffold runs against a broken build until #41 lands. ~10 min.
3. **content-posting-lab — rebase or abandon local `feature/sounds-message-html-links`.** `[gone]` upstream, now 19+ commits behind reality. The `lazy-Rosana/` work needs an intent call. ~15 min.
4. **claude-monorepo — commit or revert the Week 1 scaffold.** Real schema migration sitting uncommitted 5 days. ~10 min.
5. **PR #5 — answer the 5 §12 questions or close.** 26 days. ~20 min.
6. **PR #24 — self-merge.** Docs-only, 5 days MERGEABLE. ~30 sec.
7. **rising-tides-course Stripe/D1 — commit or stash.** 28 days. ~10 min.
8. **Anthropic billing thread — escalate or cap.** 6 days no reply. ~5 min.

---

## 📦 ACTIVE PROJECT STATE (live, verified just now)

| Project | Branch | Dirty | Local vs origin | Origin since 5/11 | Deployed SHA drift |
|---|---|---|---|---|---|
| `claude-monorepo` | gitbutler/workspace | yes (5 mods + new migration + standups/) | 9 ahead / 1 behind origin/main | 0 commits | unable to check (no deploy probe) |
| `risingtides-campaign-hub` | feat/link-tracker-to-campaign | yes (7 mods + .claude/) | 0 / 4 vs fork | 0 commits to main | unable to check |
| `content-posting-lab` | feature/sounds-message-html-links | yes (`lazy-Rosana/`) | upstream `[gone]` | **+4 commits to main since 5/11** | unable to check |
| `rising-tides-course` | customer-support | yes (Stripe/D1) | +3 / -10 | 0 commits | unable to check |
| `tidestracker` | main | yes (`.claude/worktrees/`) | 0 / 0 | 0 commits | unable to check |

---

## 🐛 OPEN CODE TODOS (active projects only)

| Project | TODO | File |
|---|---|---|
| `risingtides-campaign-hub` | decline endpoint incomplete | `frontend/src/pages/CampaignOutreach.tsx:655` |
| `risingtides-campaign-hub` | `update_campaign_fields` missing from db.py | `docs/plans/2026-02-22-campaign-hub-implementation.md:709` |
| `risingtides-campaign-hub` | save extended fields (notion_page_id, label) | `docs/plans/2026-02-22-campaign-hub-implementation.md:938` |
| `tidestracker` | 11 placeholder TODOs in `worktree-polished/src/utils/config.py` (still) | multiple |

(Filtered out stale repos: viral-course*, tui-sonnet, video-companion, tldraw-agent, ai, etc.)

---

## 🏃 RUNNING INFRA (sanity)

- 🟥 **brain-postgres / brain MCP** — fully unreachable, all calls failed.
- ⚠️ **Dev servers** — `:3099`, `:3737`, `:18789`, `:18791`, `:18792`, `:19876` listening. Without brain's session→PID mapping, can't safely attribute orphans. Not killing.
- ✅ **gh / git** — healthy.
- ✅ **Gmail** — healthy. Email scan dry today (no billing/error alerts in last 48h). Daytona sent a heads-up about a May 24 breaking change to their SDK pagination — informational, file if you use Daytona.

---

## 🗂️ BACKLOG PULSE

Brain alerts unavailable today. Last known: 39 open alerts in brain, ~35 urgent-flagged stale-repo noise.

---

## 📮 DATA SOURCE STATUS

- **Brain MCP** — fully DOWN. All 7 calls failed silently. **5th brain-degraded standup in 17 days.**
- **Live git** — fully healthy ✅ — caught everything brain missed.
- **`gh` per-org PR sweep** — healthy ✅, surfaced PR #12 closure + PRs #41/#42 opened today.
- **Gmail (search_threads)** — healthy ✅, returned dry result for billing/error filter (no fresh alerts).
- **iMessage** — skipped (no allowlist file, 8th confirmed standup).
- **Deploy probes (Railway / Vercel / Cloudflare / Supabase)** — skipped (11th carry — not in pipeline yet).

---

## 📈 PATTERN

- **WOs are write-only.** WO-01/02/03/05 are all at 0% DoD after 16–18 days. WO-04 is the only one that ever moved (and only because the standup auto-marked it superseded on 5/07). The work-order format is not driving closure; it's accumulating debt.
- **Brain is structurally unreliable.** 5 of the last 17 standups have been brain-degraded (down or stale). The standup pipeline now has to assume brain is optional, not authoritative.
- **PR-decision queue is the load-bearing block.** 8 of today's 8 LIVE ISSUES require John to decide something. The standup correctly cannot close them, and the things it CAN do (gitignore PRs, WO supersede, DoD ticking) are low-cost hygiene — useful, but they don't move the queue.
- **content-posting-lab is shipping fast without local sync.** Now 19+ origin/main commits in 5 days while the local feature branch is dead-stale. Either John is shipping from a different machine/worktree brain doesn't index, or another collaborator is. Either way the local repo at `/Users/risingtidesdev/dev/content-posting-lab` is no longer a useful working copy.

---

## ⚙️ STANDUP PIPELINE TODOs

Appended to `STANDUP_IMPROVEMENT_NOTES.md`:

- **Brain-down resilience drill passed today.** The 4/28 + 5/11 + 5/12 sequence proves the live-git fallback works. Formalize: if `brain_doctor` fails, immediately run the live-`git`+`gh`+Gmail bundle in parallel — already done today, but should be the spec, not improvisation.
- **DoD audit bash script broke on zsh** — `status` is a readonly variable in zsh. Trivial fix: rename the var to `wo_status`. ~30 sec.
- **PR-decision-queue forcing function still missing.** Today's standup correctly cannot close any of the 8 NEEDS JOHN items. But the same items have been NEEDS JOHN for 2–4 standups. A Telegram blast at standup-N for any single PR/branch that has been NEEDS JOHN for N+ standups would force closure.
- **Daytona-style "external breaking change" emails** are a new signal class. Today's `nikola@daytona.io` notice about May 24 SDK changes is worth surfacing somewhere — not LIVE ISSUE, not noise. Possibly a new "📅 EXTERNAL DEADLINES" section.

---

_Generated by morning-stand-up scheduled task | 2026-05-12_
